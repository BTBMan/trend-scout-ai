/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { ALPHA_STAMP_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const STAMP_ALPHA_DISCRIMINATOR = new Uint8Array([
  111, 217, 89, 195, 213, 235, 95, 186,
]);

export function getStampAlphaDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(STAMP_ALPHA_DISCRIMINATOR);
}

export type StampAlphaInstruction<
  TProgram extends string = typeof ALPHA_STAMP_PROGRAM_ADDRESS,
  TAccountFinder extends string | AccountMeta<string> = string,
  TAccountAlphaStamp extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFinder extends string
        ? WritableSignerAccount<TAccountFinder> &
            AccountSignerMeta<TAccountFinder>
        : TAccountFinder,
      TAccountAlphaStamp extends string
        ? WritableAccount<TAccountAlphaStamp>
        : TAccountAlphaStamp,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type StampAlphaInstructionData = {
  discriminator: ReadonlyUint8Array;
  url: string;
  score: number;
  urlSeed: ReadonlyUint8Array;
};

export type StampAlphaInstructionDataArgs = {
  url: string;
  score: number;
  urlSeed: ReadonlyUint8Array;
};

export function getStampAlphaInstructionDataEncoder(): Encoder<StampAlphaInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["url", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["score", getU8Encoder()],
      ["urlSeed", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: STAMP_ALPHA_DISCRIMINATOR }),
  );
}

export function getStampAlphaInstructionDataDecoder(): Decoder<StampAlphaInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["url", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["score", getU8Decoder()],
    ["urlSeed", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getStampAlphaInstructionDataCodec(): Codec<
  StampAlphaInstructionDataArgs,
  StampAlphaInstructionData
> {
  return combineCodec(
    getStampAlphaInstructionDataEncoder(),
    getStampAlphaInstructionDataDecoder(),
  );
}

export type StampAlphaAsyncInput<
  TAccountFinder extends string = string,
  TAccountAlphaStamp extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  finder: TransactionSigner<TAccountFinder>;
  alphaStamp?: Address<TAccountAlphaStamp>;
  systemProgram?: Address<TAccountSystemProgram>;
  url: StampAlphaInstructionDataArgs["url"];
  score: StampAlphaInstructionDataArgs["score"];
  urlSeed: StampAlphaInstructionDataArgs["urlSeed"];
};

export async function getStampAlphaInstructionAsync<
  TAccountFinder extends string,
  TAccountAlphaStamp extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ALPHA_STAMP_PROGRAM_ADDRESS,
>(
  input: StampAlphaAsyncInput<
    TAccountFinder,
    TAccountAlphaStamp,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  StampAlphaInstruction<
    TProgramAddress,
    TAccountFinder,
    TAccountAlphaStamp,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ALPHA_STAMP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    finder: { value: input.finder ?? null, isWritable: true },
    alphaStamp: { value: input.alphaStamp ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.alphaStamp.value) {
    accounts.alphaStamp.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([97, 108, 112, 104, 97])),
        getAddressEncoder().encode(expectAddress(accounts.finder.value)),
        fixEncoderSize(getBytesEncoder(), 32).encode(expectSome(args.urlSeed)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.finder),
      getAccountMeta(accounts.alphaStamp),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getStampAlphaInstructionDataEncoder().encode(
      args as StampAlphaInstructionDataArgs,
    ),
    programAddress,
  } as StampAlphaInstruction<
    TProgramAddress,
    TAccountFinder,
    TAccountAlphaStamp,
    TAccountSystemProgram
  >);
}

export type StampAlphaInput<
  TAccountFinder extends string = string,
  TAccountAlphaStamp extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  finder: TransactionSigner<TAccountFinder>;
  alphaStamp: Address<TAccountAlphaStamp>;
  systemProgram?: Address<TAccountSystemProgram>;
  url: StampAlphaInstructionDataArgs["url"];
  score: StampAlphaInstructionDataArgs["score"];
  urlSeed: StampAlphaInstructionDataArgs["urlSeed"];
};

export function getStampAlphaInstruction<
  TAccountFinder extends string,
  TAccountAlphaStamp extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ALPHA_STAMP_PROGRAM_ADDRESS,
>(
  input: StampAlphaInput<
    TAccountFinder,
    TAccountAlphaStamp,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): StampAlphaInstruction<
  TProgramAddress,
  TAccountFinder,
  TAccountAlphaStamp,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ALPHA_STAMP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    finder: { value: input.finder ?? null, isWritable: true },
    alphaStamp: { value: input.alphaStamp ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.finder),
      getAccountMeta(accounts.alphaStamp),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getStampAlphaInstructionDataEncoder().encode(
      args as StampAlphaInstructionDataArgs,
    ),
    programAddress,
  } as StampAlphaInstruction<
    TProgramAddress,
    TAccountFinder,
    TAccountAlphaStamp,
    TAccountSystemProgram
  >);
}

export type ParsedStampAlphaInstruction<
  TProgram extends string = typeof ALPHA_STAMP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    finder: TAccountMetas[0];
    alphaStamp: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: StampAlphaInstructionData;
};

export function parseStampAlphaInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedStampAlphaInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      finder: getNextAccount(),
      alphaStamp: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getStampAlphaInstructionDataDecoder().decode(instruction.data),
  };
}
